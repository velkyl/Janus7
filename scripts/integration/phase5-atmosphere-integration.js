/**
 * Phase 5 Atmosphere Integration
 *
 * Registers Phase-5 (Atmosphere) layer so it can attach to `janus7Ready`.
 *
 * NOTE:
 * - This file must not have UI side effects.
 * - It is safe to import early; the implementation is hook-driven.
 */

import '../../atmosphere/phase5.js';
